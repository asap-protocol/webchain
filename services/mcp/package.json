{
  "name": "@webchain/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    "./test-support/bootstrap-mcp-stack": "./src/test-support/bootstrap-mcp-stack.ts",
    "./test-support/mcp-client-helpers": "./src/test-support/mcp-client-helpers.ts"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:integration": "vitest run --config vitest.integration.config.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "@webchain/protocol": "workspace:*",
    "zod": "^4.3.6",
    "zod-to-json-schema": "^3.25.1"
  },
  "devDependencies": {
    "@webchain/companion": "workspace:*",
    "@webchain/runtime": "workspace:*"
  }
}
